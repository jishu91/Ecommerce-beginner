<nav class="bg-white/90 backdrop-blur sticky top-0 z-50 shadow-sm border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">

            <!-- Logo -->
            <a href="/products"
                class="flex items-center gap-2 text-2xl font-extrabold text-indigo-600 hover:text-indigo-700 transition">
                🛒 <span>MyShop</span>
            </a>

            <!-- Links -->
            <div class="flex items-center gap-6 text-sm font-medium">

                <a href="/products" class="text-gray-700 hover:text-indigo-600 transition">
                    Products
                </a>

                <% if (user) { %>

                    <a href="/orders" class="text-gray-700 hover:text-indigo-600 transition">
                        My Orders
                    </a>

                    <!-- Cart -->
                    <a href="/cart" class="relative text-gray-700 hover:text-indigo-600 transition">
                        🛒 Cart
                        <!-- Badge (future use) -->
                        <span
                            class="absolute -top-2 -right-3 bg-indigo-600 text-white text-xs w-5 h-5 flex items-center justify-center rounded-full">
                            0
                        </span>
                    </a>

                    <!-- User -->
                    <div class="flex items-center gap-2 bg-gray-100 px-3 py-1 rounded-full">
                        <div
                            class="w-7 h-7 rounded-full bg-indigo-600 text-white flex items-center justify-center text-xs font-bold uppercase">
                            <%= user.name.charAt(0) %>
                        </div>
                        <span class="text-gray-700 font-semibold">
                            <%= user.name %>
                        </span>
                    </div>

                    <!-- Logout -->
                    <a href="/logout" class="text-red-500 hover:text-red-600 transition font-semibold">
                        Logout
                    </a>

                    <% } else { %>

                        <a href="/login" class="text-gray-700 hover:text-indigo-600 transition">
                            Login
                        </a>

                        <a href="/register"
                            class="bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition">
                            Register
                        </a>

                        <% } %>
            </div>
        </div>
    </div>
</nav>